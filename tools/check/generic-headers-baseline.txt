# Cabeceras genericas con tipos concretos, pendientes de reubicar. Un fichero AQUI esta
# aceptado de forma explicita; cualquier fichero NUEVO con un tipo concreto falla.
# La ruta es relativa a engine/include/eng/ (p. ej. `core/foo.hpp`).
#
# Vacio: `affine.hpp` se renombro a `fixed_affine.hpp` (el nombre declara la dependencia de `Fixed`,
# como `fixed.hpp`). Mantener este fichero sin entradas: si algo aparece aqui, es deuda a justificar.
